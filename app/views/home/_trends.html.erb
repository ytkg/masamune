<div class="card">
  <h2 class="card-header">トレンド</h2>
  <div class="card-body">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">キーワード</th>
        </tr>
      </thead>
      <tbody>
        <% @trends.each.with_index(1) do |trend, i| %>
          <tr>
            <th scope="row"><%= i %></th>
            <td><%= link_to trend.name, trend.url, target: '_blank' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
